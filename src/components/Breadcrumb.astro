---
const currentUrlPath = Astro.url.pathname.replace(/\/+$/, "");
const breadcrumbList = currentUrlPath.split("/").slice(1);

switch (breadcrumbList[0]) {
  case "posts":
    breadcrumbList.splice(0, 2, `게시글 (페이지 ${breadcrumbList[1] || 1})`);
    break;

  case "projects":
    breadcrumbList.splice(0, 2, `프로젝트 (페이지 ${breadcrumbList[1] || 1})`);
    break;

  case "tags":
    if (!isNaN(Number(breadcrumbList[2]))) {
      breadcrumbList.splice(
        1,
        3,
        `${breadcrumbList[1]} ${Number(breadcrumbList[2]) === 1 ? "" : "(page " + breadcrumbList[2] + ")"}`
      );
    } else {
      breadcrumbList.splice(0, 2, "태그");
    }
    break;

  case "archives":
    breadcrumbList.splice(0, 2, "아카이브");
    break;

  case "search":
    breadcrumbList.splice(0, 2, "검색");
    break;
}
---

<nav class="mx-auto mt-8 mb-1 w-full max-w-3xl px-4" aria-label="breadcrumb">
  <ul
    class="font-light [&>li]:inline [&>li:not(:last-child)>a]:hover:opacity-100"
  >
    <li>
      <a href="/" class="opacity-80">홈</a>
      <span aria-hidden="true" class="opacity-80">&raquo;</span>
    </li>
    {
      breadcrumbList.map((breadcrumb, index) =>
        index + 1 === breadcrumbList.length ? (
          <li>
            <span
              class:list={["capitalize opacity-75", { lowercase: index > 0 }]}
              aria-current="page"
            >
              {/* make the last part lowercase in Home > Tags > some-tag */}
              {decodeURIComponent(breadcrumb)}
            </span>
          </li>
        ) : (
          <li>
            <a href={`/${breadcrumb}/`} class="capitalize opacity-70">
              {breadcrumb}
            </a>
            <span aria-hidden="true">&raquo;</span>
          </li>
        )
      )
    }
  </ul>
</nav>
