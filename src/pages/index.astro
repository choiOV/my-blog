---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import Socials from "@/components/Socials.astro";
import LinkButton from "@/components/LinkButton.astro";
import Card from "@/components/Card.astro";
import Hr from "@/components/Hr.astro";
import getSortedEntries from "@/utils/getSortedEntries";
import IconRss from "@/assets/icons/IconRss.svg";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import { SITE } from "@/config";
import { SOCIALS } from "@/constants";

const posts = await getCollection("blog");
const projects = await getCollection("projects");
const books = await getCollection("books");

const sortedPosts = getSortedEntries(posts);
const sortedProjects = getSortedEntries(projects);
const sortedBooks = getSortedEntries(books);

// const featuredPosts = sortedPosts.filter(({ data }) => data.featured);
const recentPosts = sortedPosts.filter(({ data }) => !data.featured);
const recentProjects = sortedProjects.filter(({ data }) => data.featured);
const recentBooks = sortedBooks.filter(({ data }) => data.featured);
---

<Layout>
  <Header />
  <main id="main-content" data-layout="index">
    <section id="hero" class="pt-8 pb-6">
      <h2 class="my-4 inline-block text-4xl font-bold sm:my-8 sm:text-4xl">
        SEOBScript
      </h2>
      <a
        target="_blank"
        href="/rss.xml"
        class="inline-block"
        aria-label="rss feed"
        title="RSS Feed"
      >
        <IconRss
          width={20}
          height={20}
          class="scale-125 stroke-accent stroke-3"
        />
        <span class="sr-only">RSS Feed</span>
      </a>

      <p>
        개발자는 혼자가 아닌 함께 일하는 사람이고, <br />
        그 함께의 무게를 감당할 자세가 되어 있어야 좋은 개발자라고 생각합니다. <br
        />
        함께하는 법을 배우고자 블로그를 시작하게 되었습니다. <br />
        의견과 피드백은 언제나 환영입니다. 감사합니다.
      </p>
      {
        SOCIALS.length > 0 && (
          <div class="mt-4 flex flex-col sm:flex-row sm:items-center">
            <Socials />
          </div>
        )
      }
    </section>

    <Hr />

    <!-- {
      featuredPosts.length > 0 && (
        <>
          <section id="featured" class="pt-12 pb-6">
            <h2 class="text-2xl font-semibold tracking-wide">추천 게시물</h2>
            <ul>
              {featuredPosts.map(data => (
                <Card variant="h3" {...data} />
              ))}
            </ul>
          </section>
          {recentPosts.length > 0 && <Hr />}
        </>
      )
    } -->

    {
      recentPosts.length > 0 && (
        <section id="recent-posts" class="pt-12 pb-6">
          <h2 class="text-2xl font-semibold tracking-wide">최근 게시글</h2>
          <ul>
            {recentPosts.map(
              (data, index) =>
                index < SITE.postPerIndex && <Card variant="h3" entry={data} />
            )}
          </ul>
        </section>
      )
    }
    {
      recentProjects.length > 0 && (
        <section id="recent-projects" class="pt-12 pb-6">
          <h2 class="text-2xl font-semibold tracking-wide">최근 프로젝트</h2>
          <ul>
            {recentProjects.map(
              (data, index) =>
                index < SITE.postPerIndex && <Card variant="h3" entry={data} />
            )}
          </ul>
        </section>
      )
    }
    {
      recentBooks.length > 0 && (
        <section id="recent-books" class="pt-12 pb-6">
          <h2 class="text-2xl font-semibold tracking-wide">최근 독후감</h2>
          <ul>
            {recentBooks.map(
              (data, index) =>
                index < SITE.postPerIndex && <Card variant="h3" entry={data} />
            )}
          </ul>
        </section>
      )
    }

    <div class="my-8 text-center">
      <LinkButton href="/archives/">
        아카이브 보러가기
        <IconArrowRight class="inline-block" />
      </LinkButton>
    </div>
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>
